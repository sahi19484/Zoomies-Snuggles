// Firebase has been removed from this project.
// Authentication is now handled via localStorage in contexts/AuthContext.tsx
// Data is stored using localStorage and mock data.

// This file is kept as a stub to avoid breaking imports.
// If you had any Firebase imports, they will need to be removed.

export const db = null;
export const auth = null;
